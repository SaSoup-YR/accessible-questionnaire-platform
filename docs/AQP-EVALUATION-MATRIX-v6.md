# AQP evaluation matrix v6

Version: 6.0
Frozen: 2026-08-09
Status rule: only evidence tied to the evaluated release is marked Evidenced

| ID | Bounded claim | Metric and operational calculation | Decision rule | Evidence source | Current evidence status |
| --- | --- | --- | --- | --- | --- |
| C1 | AQP implements bounded definition-driven reuse: supported questionnaire content is loaded through a shared runner and workflow, while response structures, scorers and optional supports remain explicit executable boundaries. It is not questionnaire-independent. | **Compatible-definition isolation:** production files changed outside definition/registration for a compatible definition ÷ all changed production files. **Shared contract reuse:** common runner/recovery/result tests executed unchanged across at least two definitions ÷ frozen common contracts. **Allowlist correctness:** observed support state matching each definition declaration ÷ checked definition × support × state combinations. | Isolation is 0% outside definition/registration for a compatible definition; 100% of eligible common contracts are reused; allowlist agreement is 100%. A new scorer or response structure is reported as executable boundary extension, not a data-only platform pass. | Architecture/contract tests; allowlist tests; change manifest for a compatible fixture. | **Partially evidenced.** Shared mechanisms and gates are implemented and tested across current definitions, but a frozen change-manifest experiment and final per-metric counts must be published for the release. The broader “questionnaire-independent” claim is withdrawn. |
| C2 | For the named release and configurations only, participant screens satisfy the pre-specified automated browser checks and manual technical assistive-technology checks. No benefit or usability claim for disabled users is made. | **Rendered browser:** passed state/profile scans ÷ 60 required scans (12 states × 5 profiles), with axe violations, incomplete checks, overflow and target size reported. **Manual AT:** passed applicable route/check cells ÷ all applicable cells on NVDA/Firefox, NVDA/Chrome, VoiceOver/Safari and one OS voice-control route; report WCAG SC outcomes and exact dynamic announcements. | All 60 browser scans present; 0 automatically detected violations; 0 overflow failures; 0 critical target-size failures; incomplete axe checks listed. Manual audit has 0 NT cells, every applicable cell P/F, and no S3 failure in a technically supported route. A markup mechanism that is not usefully announced is F. | Playwright/axe JSON+HTML and trace artefact; cross-browser matrix; versioned manual AT audit. | **Not evidenced yet for the release.** Browser suite is implemented but has not run in this local environment; manual audit protocol is frozen but all route cells remain NT. Therefore “accessible”, “more accessible” and disabled-user benefit claims are unavailable. |
| C3 | Within the frozen support boundary, AQP preserves item content/order, response labels/stored values, required status, scoring identifier and reported score from source to rendered response and result. | **Fidelity mismatch rate:** unequal expected/observed fields ÷ all independent-oracle comparisons, reported for every built-in instrument and every QSF/LSS/LSG/LSQ fixture. | 0 mismatches in every case. One missing, reordered, relabelled, revalued or differently scored field fails the release. | Hand-maintained source-of-truth JSON; programmatic production-path round trip; machine-readable report. | **Locally evidenced; release verification pending.** 9 cases, 39 items, 288 field comparisons, 0 mismatches. Must be reproduced for the deployed commit. |
| C4 | AQP fails closed for the pre-specified unsupported or adversarial inputs and does not silently alter them into participant-ready questionnaires. | **Negative outcome count:** number in each of refused-specific, refused-generic, accepted-documented-loss and silently-altered across 12 frozen adversarial inputs. | Silently altered = 0/12. Every accepted loss must be explicit and acknowledged; every refusal must produce no participant link/partial definition. Specific and generic refusals are reported separately. | Automated negative battery and row-level results table. | **Locally evidenced; release verification pending.** 10 specific refusals, 2 generic parse refusals, 0 accepted documented losses, 0 silently altered. |

## Active claims (continued)

| ID | Bounded claim | Metric and operational calculation | Decision rule | Evidence source | Current evidence status |
| --- | --- | --- | --- | --- | --- |
| C5 | The revised researcher setup can be evaluated formatively against a precisely matched Qualtrics setup condition for task success, time, assists, errors and perceived ease. This is an optional additional study, not required for the technical dissertation evaluation. | Per condition: valid task completions ÷ assigned tasks; active time; assists; predefined errors; discrepancy detection; post-task SEQ. Post-session SUS. Report each participant and median/range. Second coder independently codes at least two recordings; report event-level agreement. | A task succeeds only when the specified end state and all validity checks are met without critical error. Assistance and errors follow frozen definitions. Report descriptive results only; no significance test or general usability claim at n=6–8. | Preregistered observed-study protocol; recordings with consent; observer logs; independent-coder sheet; SEQ/SUS forms. | **Not evidenced.** No sessions may run without amendment approval. If approval/timing does not permit, retain the fully specified protocol as planned work and keep C5 Not evidenced. |
| C7 | Each result record is bound to the questionnaire definition used for its configuration, and the definition, final response set and score can be reconstructed and checked from the export alone. | **Hash verification:** matching canonical SHA-256 in configuration, recomputed loaded definition and submitted result ÷ tested cases; tampered configurations blocked ÷ tamper cases. **Reconstruction:** fully reconstructed exports with 0 definition/response/score mismatches ÷ all fidelity cases. | Hash agreement 100%; every hash mismatch blocks submission and cannot show success. Reconstruction 9/9 with 0 mismatches; every export contains the complete definition, scorer and responses needed without repository lookup. | Configuration/result schema tests; tampered-link tests; export reconstruction report. | **Locally evidenced; release verification pending.** Hash checks pass in unit/component tests; 9/9 exports and 39/39 item responses reconstructed with 0 mismatches. Must be reproduced and deployed before the claim is marked Evidenced. |

## Removed row

**C6 (AQP versus matched Qualtrics participant accessibility comparison) is
removed.** The withdrawn design confounded presentation structure and did not
provide interpretable mechanism-level or score-equivalence evidence. It is not
retained as an unevidenced active claim.
