name: RF-09 one-time final-status preview publisher

on:
  pull_request:

permissions:
  contents: write

jobs:
  publish:
    if: github.event.pull_request.number == 77
    runs-on: ubuntu-24.04
    steps:
      - name: Check out exact synchronized runtime
        uses: actions/checkout@v4
        with:
          ref: 30f001429ba67b85099e13267061f4b7dd317ac2
          path: runtime
          fetch-depth: 1

      - name: Check out gh-pages separately
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: pages
          fetch-depth: 1

      - name: Create only the new immutable preview
        shell: bash
        run: |
          set -euo pipefail
          PREVIEW='pages/rf09-final-status-preview'
          if [ -e "$PREVIEW" ]; then
            echo 'Refusing to overwrite an existing RF-09 final-status preview.' >&2
            exit 1
          fi
          mkdir -p "$PREVIEW"
          cp runtime/source/demo/accessible-questionnaire-platform-v0.8.html "$PREVIEW/index.html"
          printf '%s\n' '30f001429ba67b85099e13267061f4b7dd317ac2' > "$PREVIEW/SOURCE-SHA.txt"

      - name: Commit and push only that preview directory
        working-directory: pages
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add rf09-final-status-preview
          CHANGED="$(git diff --cached --name-only)"
          printf '%s\n' "$CHANGED"
          test -n "$CHANGED"
          if printf '%s\n' "$CHANGED" | grep -v '^rf09-final-status-preview/' | grep -q .; then
            echo 'Unexpected gh-pages path changed; refusing publish.' >&2
            exit 1
          fi
          git commit -m 'Publish immutable RF-09 final-status preview 30f00142'
          git push origin HEAD:gh-pages
