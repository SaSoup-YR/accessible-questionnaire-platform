name: One-time RF-07 dictation-quality synchronization

on:
  pull_request:

permissions:
  contents: write

jobs:
  synchronize:
    if: github.event.pull_request.head.ref == 'agent/retry-rf07-on-device-speech'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: source
    steps:
      - name: Check out the RF-07 successor branch
        uses: actions/checkout@v4
        with:
          ref: agent/retry-rf07-on-device-speech
          fetch-depth: 5

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: source/package-lock.json

      - name: Install locked dependencies
        run: npm ci

      - name: Run all unit and component tests
        run: npm test -- --run

      - name: Build and synchronize release outputs
        run: npm run build:release

      - name: Remove one-time workflow and check formatting
        run: |
          rm ../.github/workflows/rf07-dictation-quality-sync.yml
          git diff --check

      - name: Commit the exact tested generated candidate
        env:
          HEAD_BRANCH: agent/retry-rf07-on-device-speech
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A \
            ../.github/workflows/rf07-dictation-quality-sync.yml \
            demo/accessible-nasa-tlx-v0.7.html \
            demo/accessible-questionnaire-platform-v0.8.html \
            ../index.html \
            ../study.html \
            ../assets \
            ../questionnaires
          if git diff --cached --quiet; then
            echo "The dictation-quality release is already synchronized."
            exit 0
          fi
          git commit -m "build: synchronize RF-07 dictation-quality release"
          git push origin HEAD:"$HEAD_BRANCH"
