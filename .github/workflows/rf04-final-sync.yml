name: RF04 final verified release sync

on:
  push:
    branches:
      - agent/fix-rf04-saved-session-recovery

permissions:
  contents: write

jobs:
  verify-and-sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: source
    steps:
      - name: Check out RF-04 branch
        uses: actions/checkout@v4
        with:
          ref: agent/fix-rf04-saved-session-recovery

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: source/package-lock.json

      - name: Install locked dependencies
        run: npm ci

      - name: Run unit and component tests
        run: npm test -- --run

      - name: Build production and release outputs
        run: npm run build:release

      - name: Commit synchronized release and remove temporary workflow
        working-directory: .
        run: |
          rm .github/workflows/rf04-final-sync.yml
          git add \
            source/demo/accessible-nasa-tlx-v0.7.html \
            source/demo/accessible-questionnaire-platform-v0.8.html \
            index.html \
            study.html \
            assets \
            questionnaires \
            .github/workflows/rf04-final-sync.yml
          if git diff --cached --quiet; then
            git add .github/workflows/rf04-final-sync.yml
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "sync final RF-04 release"
          git push origin HEAD:agent/fix-rf04-saved-session-recovery
